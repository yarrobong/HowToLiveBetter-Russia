# Полная русификация HowToLiveBetter-Russia — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Убрать иностранное локальное наследие исходной редакции из актуального дерева репозитория, сохранив 31 рабочий раздел, русскую навигацию и зелёный CI.

**Architecture:** Сначала тестами фиксируется новый контракт: русские пути, русский machine-tag и отсутствие CJK. Затем 31 файл `book/` атомарно переименовываются и одновременно получают новый hidden-tag формат. После этого обновляются README, основные docs, `CLAUDE.md`, checker и UI; legacy-документы удаляются. Финал — полный CJK-scan, unit tests, repository checker, PR и проверка `main` после merge.

**Tech Stack:** Markdown, статический HTML/JavaScript, Python 3.12, GitHub Actions, GitHub Git Data API.

**Spec:** `docs/superpowers/specs/2026-09-17-полная-русификация.md`

## Global Constraints

- В `book/` после миграции ровно 31 `NN-*.md`.
- Ни один активный текстовый файл и ни один активный путь не содержит CJK.
- Hidden-tag: `<!-- метаданные: деньги=0|мало|много время=мало|средне|много усилие=нет|немного|да польза=высокая|средняя|низкая метрика=здоровье|деньги|время|свобода -->`.
- Старый формат hidden-tag больше не принимается.
- README содержит 31 существующую ссылку на русские пути `book/`.
- Российские нормы сохраняют freshness-маркер `Актуальность РФ проверена:`.
- История fork не переписывается; удалённое наследие остаётся доступно через Git history.
- `LICENSE` не переводится.

---

### Task 1: Новый русский контракт

**Files:**
- Modify: `tools/test_check_russia_adaptation.py`
- Modify: `tools/check_russia_adaptation.py`
- Modify: `.github/workflows/russia-adaptation-check.yml`

- [ ] Добавить failing-тесты на русский metadata-tag, запрет старого формата, русские пути 31 раздела и отсутствие CJK.
- [ ] Подтвердить RED в CI до миграции.
- [ ] Реализовать минимальный checker нового контракта.
- [ ] Настроить CI для ветки `full-russian-localization`.

### Task 2: Переименовать 31 раздел и перевести machine-tags

**Files:**
- Rename/modify: все `book/01-*.md` … `book/31-*.md` по карте из spec.

**Новая схема метаданных:**
- `деньги=0|мало|много`
- `время=мало|средне|много`
- `усилие=нет|немного|да`
- `польза=высокая|средняя|низкая`
- `метрика=здоровье|деньги|время|свобода`

- [ ] Создать deterministic mapping старых путей на русские пути из spec.
- [ ] Преобразовать каждый legacy hidden-tag в новый формат.
- [ ] Переименовать 31 файл без промежуточных дублей.
- [ ] Проверить recursive tree: ровно 31 `book/NN-*.md`, пути без CJK.

### Task 3: README и основные русские docs

**Files:**
- Modify: `README.md`
- Rename: `docs/СТАТУС-АДАПТАЦИИ.md` → `docs/СТАТУС-АДАПТАЦИИ.md`
- Rename: `docs/ИСТОЧНИКИ-РФ.md` → `docs/ИСТОЧНИКИ-РФ.md`
- Modify: `index.html`

- [ ] Обновить все 31 ссылки README.
- [ ] Переименовать два основных документа и заменить активные ссылки.
- [ ] Убедиться, что `index.html` использует новые README-пути и полностью русский UI.

### Task 4: Переписать `CLAUDE.md` и финализировать checker

**Files:**
- Replace: `CLAUDE.md`
- Modify: `tools/check_russia_adaptation.py`
- Modify: `tools/test_check_russia_adaptation.py`

- [ ] Оставить в `CLAUDE.md` только актуальные правила российской редакции.
- [ ] Checker принимает только русский metadata-tag.
- [ ] Checker сканирует активные `.md`, `.html`, `.py`, `.yml`, `.yaml`, `.xml`, `.txt`, `.json`, `.css`, `.js` на CJK.
- [ ] `LICENSE` исключается из текстового CJK-scan.

### Task 5: Удалить legacy-наследие

**Files:**
- Delete: старая папка research-notes исходной редакции.
- Delete: standalone docs с CJK-путями.
- Delete: старые wave plans/reviews/spec, кроме текущих spec и plan.
- Delete: stale `og.png` и `tools/og.html`, если их русская версия не гарантирована.

- [ ] Удалить все CJK-пути из активного дерева.
- [ ] Убедиться, что активные README/index/docs не ссылаются на удалённые файлы.

### Task 6: Финальная верификация

- [ ] Run: `python -m unittest tools.test_check_russia_adaptation -v` — ожидается PASS.
- [ ] Run: `python tools/check_russia_adaptation.py` — ожидается `Russia adaptation check: OK`.
- [ ] CJK count по активным текстам и путям = 0.
- [ ] README: ровно 31 существующая ссылка на `book/`.
- [ ] Branch CI = success.

### Task 7: PR и merge

- [ ] Compare `main...full-russian-localization` и проверить scope.
- [ ] Открыть PR `Полная русификация репозитория`.
- [ ] Дождаться зелёного PR CI и `mergeable=true`.
- [ ] Squash merge в `main`.
- [ ] Проверить CI на merge-коммите `main`.
