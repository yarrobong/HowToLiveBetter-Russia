# Полная русификация HowToLiveBetter-Russia — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Убрать китайские имена, служебные теги и исторические документы из актуального дерева репозитория, сохранив 31 рабочий раздел, русскую навигацию и зелёный CI.

**Architecture:** Сначала тестами фиксируется новый контракт: русские пути, русский machine-tag и отсутствие CJK. Затем 31 файл `book/` атомарно переименовываются и одновременно получают новый hidden-tag формат. После этого обновляются README, основные docs, `CLAUDE.md`, checker и UI; legacy-документы удаляются одним tree-коммитом. Финал — полный CJK-scan, unit tests, repository checker, PR и проверка `main` после merge.

**Tech Stack:** Markdown, статический HTML/JavaScript, Python 3.12, GitHub Actions, GitHub Git Data API.

**Spec:** `docs/superpowers/specs/2026-09-17-полная-русификация.md`

## Global Constraints

- В `book/` после миграции ровно 31 `NN-*.md`.
- Ни один активный текстовый файл не содержит китайских иероглифов.
- Новый hidden-tag: `<!-- метаданные: деньги=0|мало|много время=мало|средне|много усилие=нет|немного|да польза=высокая|средняя|низкая метрика=здоровье|деньги|время|свобода -->`.
- Старый hidden-tag больше не принимается.
- README содержит 31 существующую ссылку на русские пути `book/`.
- Российские нормы сохраняют freshness-маркер `Актуальность РФ проверена:`.
- История fork не переписывается; удалённое наследие остаётся доступно через Git history.
- `LICENSE` не переводится.

---

### Task 1: RED-тесты нового русского контракта

**Files:**
- Modify: `tools/test_check_russia_adaptation.py`
- Modify: `tools/check_russia_adaptation.py`

**Interfaces:**
- Produces: `contains_cjk(text: str) -> bool`, `scan_active_text_files(root: Path) -> list[Path]`, новый `validate_cost_tag()`.

- [ ] **Step 1: Добавить падающие тесты**

Добавить проверки, что:

```python
self.assertEqual([], find_cjk_paths(root))
self.assertEqual([], validate_cost_tag(
    '<!-- метаданные: деньги=0 время=мало усилие=нет польза=высокая метрика=деньги -->'
))
self.assertTrue(validate_cost_tag(
    '<!-- legacy-tag -->'
))
```

Также добавить тест, что все README-пути `book/` не содержат CJK.

- [ ] **Step 2: Запустить тесты и получить RED**

Run: `python -m unittest tools.test_check_russia_adaptation -v`

Expected: FAIL — новый русский тег и CJK scan ещё не реализованы.

- [ ] **Step 3: Не чинить production-код до подтверждённого RED**

Зафиксировать RED в CI ветки.

---

### Task 2: Атомарно переименовать 31 файл `book/` и перевести machine-tags

**Files:**
- Rename/modify: все `book/01-*.md` … `book/31-*.md` по карте из spec.

**Interfaces:**
- Consumes: карта путей из design spec.
- Produces: ровно 31 русских Markdown-файла с неизменным пользовательским содержанием карточек и русским hidden-tag форматом.

- [ ] **Step 1: Создать deterministic mapping старый путь → новый путь**

Использовать точную таблицу из `docs/superpowers/specs/2026-09-17-полная-русификация.md`.

- [ ] **Step 2: Преобразовать hidden-tags**

Сопоставление значений:

```text
money: 0→0, low→мало, high→много
সময়: low→мало, medium→средне, high→много
усилие: no→нет, some→немного, yes→да
польза: high→высокая, medium→средняя, low→низкая
метрика: mortality/health→здоровье, money→деньги, time→время, freedom→свобода
```

В коде преобразования использовать фактические значения старой схемы из checker, а не английские подписи выше.

- [ ] **Step 3: Создать новые blobs**

Для каждого из 31 файлов создать новый blob с тем же содержанием карточек и заменённым machine-tag.

- [ ] **Step 4: Создать один Git tree**

В одном tree-коммите:
- добавить 31 новый русский путь;
- удалить 31 старый путь.

Expected: после commit `book/` содержит ровно 31 файл, без промежуточных дублей.

- [ ] **Step 5: Проверить tree**

Проверить recursive tree: 31 файлов `book/NN-*.md`, все пути без CJK.

---

### Task 3: README и основные русские docs

**Files:**
- Modify: `README.md`
- Rename: `docs/RUSSIA-MIGRATION.md` → `docs/СТАТУС-АДАПТАЦИИ.md`
- Rename: `docs/RUSSIA-SOURCES.md` → `docs/ИСТОЧНИКИ-РФ.md`
- Modify: `index.html`

**Interfaces:**
- Produces: 31 корректных README-ссылок, русские названия пользовательских документов.

- [ ] **Step 1: Обновить 31 README-ссылку**

Каждый номер должен ссылаться на новый путь из spec.

- [ ] **Step 2: Обновить ссылки на основные docs**

Во всех активных файлах заменить `RUSSIA-MIGRATION.md` и `RUSSIA-SOURCES.md` на новые русские имена.

- [ ] **Step 3: Переименовать два docs атомарно**

Содержимое сохраняется русским; старые английские пути удаляются.

- [ ] **Step 4: Обновить `index.html`**

UI остаётся русским, получает новые пути через README. Удалить любые legacy-словарики или старые machine-tag ожидания.

---

### Task 4: Переписать `CLAUDE.md` и checker под русский контракт

**Files:**
- Replace: `CLAUDE.md`
- Modify: `tools/check_russia_adaptation.py`
- Modify: `tools/test_check_russia_adaptation.py`

**Interfaces:**
- Produces: checker, который принимает только русский machine-tag и сканирует CJK.

- [ ] **Step 1: Переписать `CLAUDE.md`**

Оставить только актуальные правила российской редакции: назначение, формат карточки, sources hierarchy, freshness, русские machine-tags, команды проверки.

- [ ] **Step 2: Реализовать новый `COST_TAG_RE`**

Точный regex:

```python
COST_TAG_RE = re.compile(
    r"<!--\s*метаданные:\s*"
    r"деньги=(?:0|мало|много)\s+"
    r"время=(?:мало|средне|много)\s+"
    r"усилие=(?:нет|немного|да)\s+"
    r"польза=(?:высокая|средняя|низкая)\s+"
    r"метрика=(?:здоровье|деньги|время|свобода)\s*-->"
)
```

- [ ] **Step 3: Реализовать CJK scan**

Сканировать активные `.md`, `.html`, `.py`, `.yml`, `.yaml`, `.xml`, `.txt`, `.json`, `.css`, `.js`.

Исключать `.git/` и удаляемые historical paths; `LICENSE` можно не сканировать.

- [ ] **Step 4: Запустить unit tests**

Run: `python -m unittest tools.test_check_russia_adaptation -v`

Expected: GREEN после миграции тегов и путей.

---

### Task 5: Удалить китайское legacy-наследие из активного дерева

**Files:**
- Delete: `docs/核实记录/` полностью
- Delete: четыре standalone китайских docs
- Delete: старые wave plans/reviews/spec, кроме новой spec и нового plan
- Delete: `og.png`, `tools/og.html` если их нельзя гарантированно русифицировать без визуальной генерации

**Interfaces:**
- Produces: актуальное дерево без CJK в путях и текстах.

- [ ] **Step 1: Сформировать список всех CJK-путей из recursive tree**

Ни один CJK path не должен остаться после commit.

- [ ] **Step 2: Удалить legacy tree одним Git tree-коммитом**

Не удалять `LICENSE`, `README.md`, новые spec/plan, основные русские docs, `book/`, checker/tests/workflow.

- [ ] **Step 3: Повторно получить recursive tree**

Expected: ни один path не содержит CJK.

---

### Task 6: Финальная верификация, PR и merge

**Files:**
- Verify: весь репозиторий
- PR: `full-russian-localization` → `main`

- [ ] **Step 1: Запустить unit tests**

Run: `python -m unittest tools.test_check_russia_adaptation -v`

Expected: PASS.

- [ ] **Step 2: Запустить repository checker**

Run: `python tools/check_russia_adaptation.py`

Expected: `Russia adaptation check: OK`.

- [ ] **Step 3: Убедиться, что CJK count = 0**

Проверка должна охватывать пути и содержимое активных текстовых файлов.

- [ ] **Step 4: Проверить README**

Expected: ровно 31 ссылки, каждая существует и ведёт на русское имя.

- [ ] **Step 5: Открыть PR**

PR body должен перечислять: 31 rename, русский metadata contract, удаление legacy docs, новый CJK gate, результаты тестов.

- [ ] **Step 6: Дождаться PR CI**

Expected: unit tests = success; repository checker = success.

- [ ] **Step 7: Squash merge**

Merge только при `mergeable=true` и зелёном PR-run.

- [ ] **Step 8: Проверить `main` после merge**

На merge-коммите повторно убедиться, что workflow = success и `README.md`/`docs/СТАТУС-АДАПТАЦИИ.md` доступны на `main`.
